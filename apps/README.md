# Apps

ポートフォリオから公開する個人開発ツールの統合先です。

- 静的HTMLアプリは各ディレクトリのソースを直接編集します。
- このディレクトリを唯一の開発元とし、旧個別リポジトリは編集しません。
- `bonus-splitter` は `source/` にNext.jsソース、直下に静的エクスポートを置きます。
- Gemini APIキーを利用するアプリでは、キーは利用者のブラウザ内に保存されます。公開コードへキーを直接記載しないでください。
